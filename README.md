# Payroll Calculator

This repository contains a payroll calculation module and a Streamlit app for interactive use.

## Streamlit app

Run the following command to start the web interface:

```bash
streamlit run payroll_app.py
```

Upload an Excel file and enter the sheet name to compute the metrics. After processing, the results can be downloaded as a new Excel file.
